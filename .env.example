############################################################
# FBAiPro - Environment template (.env for local only)
# Do NOT commit real secrets. In Vercel, set these in:
# Project Settings > Environment Variables (Preview + Production)
############################################################

# 1) App base URLs
# - Local dev URL; Vercel will set this per-deployment
NEXTAUTH_URL=http://localhost:3000
# - Required secret for NextAuth (generate a strong random string)
NEXTAUTH_SECRET=

# 2) Database (choose ONE for local; in Vercel use Supabase/Postgres)
# SQLite for local dev (default schema uses sqlite)
DATABASE_URL="file:./dev.db"
# Supabase Postgres (Production/Preview on Vercel)
# In Vercel, set DATABASE_URL to your Supabase connection string (Prisma format)
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public&sslmode=require"

# 3) Upstash (Redis or REST/QStash) — pick what you use
# For BullMQ/Redis client
REDIS_URL=
# OR Upstash REST API (if you prefer REST access)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Optional: QStash (HTTP-based task queue + webhooks)
QSTASH_URL=https://qstash.upstash.io
QSTASH_TOKEN=
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# 4) Cloudflare R2 (S3-compatible object storage)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# Endpoint usually: https://<accountid>.r2.cloudflarestorage.com
R2_ENDPOINT=
# Optional public base URL (Cloudflare CDN / custom domain)
NEXT_PUBLIC_R2_PUBLIC_BASE_URL=

# 5) Optional AI providers
# Hugging Face Inference API (optional, free-tier)
HUGGINGFACE_API_TOKEN=

# 6) Amazon Seller Partner API (optional, not used yet)
AMAZON_SP_API_REFRESH_TOKEN=
AMAZON_SP_API_CLIENT_ID=
AMAZON_SP_API_CLIENT_SECRET=
# Region codes: na | eu | fe
AMAZON_SP_API_REGION=na

############################################################
# How to use
# - Local: copy this file to .env and fill what you need for development
#   Windows PowerShell:
#   Copy-Item .env.example .env
# - Vercel: set the same variables in Project Settings > Environment Variables
#   - Production + Preview environments
#   - Do NOT upload your local .env to the repo
#
# Database note:
# - For production, switch Prisma to Postgres (prisma/schema.prisma provider="postgresql")
#   and set DATABASE_URL to the Supabase connection string in Vercel.
# - Keep SQLite locally if you prefer fast dev; it’s fine.
############################################################
