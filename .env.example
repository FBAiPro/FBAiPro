# App
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace_me_with_a_secure_random_string

# Database
# For local development, SQLite (default in prisma/schema.prisma)
DATABASE_URL="file:./dev.db"

# Optional: switch to Postgres (Neon/Supabase) later
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"

# Optional: Hugging Face Inference API (free-tier)
HUGGINGFACE_API_TOKEN=hf_...

# Amazon APIs (stubs for later)
AMAZON_SP_API_REFRESH_TOKEN=
AMAZON_SP_API_CLIENT_ID=
AMAZON_SP_API_CLIENT_SECRET=
AMAZON_SP_API_REGION=na
